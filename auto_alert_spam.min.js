javascript:(function () { function createProgressContainer() { var progressDiv = document.createElement('div'); progressDiv.id = 'progressContainer'; Object.assign(progressDiv.style, { position: 'fixed', bottom: '20px', right: '20px', backgroundColor: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 15px', borderRadius: '8px', fontSize: '14px', zIndex: '9999', whiteSpace: 'pre-line' }); document.body.appendChild(progressDiv); return progressDiv; } function updateProgress(div, current, total, msg) { div.innerText = '実行状況: ' + current + (total === '—' ? ' 件' : ' / ' + total) + '\n' + msg; } function getTweetDate(container) { var timeEl = container.querySelector('time[datetime]'); if (!timeEl) return null; var dt = new Date(timeEl.getAttribute('datetime')); return isNaN(dt.getTime()) ? null : dt; } function findElementByText(root, selector, texts) { root = root || document; var list = root.querySelectorAll(selector); for (var i = 0; i < list.length; i++) { var el = list[i]; if (!document.body.contains(el)) continue; var t = (el.innerText || el.textContent || '').trim(); if (!t) continue; for (var j = 0; j < texts.length; j++) { if (t.indexOf(texts[j]) !== -1) { return el; } } } return null; } function run(startDateStr, endDateStr, waitTimeStr, maxCountStr) { var waitTime = Math.max(1, parseInt(waitTimeStr, 10) || 15) * 1000; var maxCount = parseInt(maxCountStr, 10); if (isNaN(maxCount) || maxCount < 1) { maxCount = 0; } var progressDiv = createProgressContainer(); var successCount = 0; var startDate = null; var endDate = null; if (startDateStr && startDateStr.trim()) { startDate = new Date(startDateStr.trim()); startDate.setHours(0, 0, 0, 0); } if (endDateStr && endDateStr.trim()) { endDate = new Date(endDateStr.trim()); endDate.setHours(23, 59, 59, 999); } var useDateFilter = startDate !== null || endDate !== null; var reloadRetryTimeout = null; var totalDisplayFn = function () { if (maxCount > 0) return maxCount; return useDateFilter ? '—' : '—'; }; var stepFn; function quickRetry() { setTimeout(function () { stepFn(); }, 500); } function scheduleRetry() { if (reloadRetryTimeout) return; if (currentInterval) { try { clearInterval(currentInterval); } catch (err) {} currentInterval = null; } if (!document.body.contains(progressDiv)) { progressDiv = createProgressContainer(); } try { updateProgress(progressDiv, successCount, totalDisplayFn(), '再読み込みを検知、待機中... (3秒後に再開)'); } catch (err) {} reloadRetryTimeout = setTimeout(function () { reloadRetryTimeout = null; try { if (!document.body.contains(progressDiv)) progressDiv = createProgressContainer(); updateProgress(progressDiv, successCount, totalDisplayFn(), '再開中...'); } catch (err) {} setTimeout(function () { try { stepFn(); } catch (err) { console.error(err); scheduleRetry(); } }, 0); }, 3000); } var mutationDebounce = null; var isActivelyProcessing = false; var observer = new MutationObserver(function (mutations) { if (isActivelyProcessing) return; var removed = 0; for (var m = 0; m < mutations.length; m++) { removed += (mutations[m].removedNodes && mutations[m].removedNodes.length) || 0; } if (removed < 3) return; if (mutationDebounce) clearTimeout(mutationDebounce); mutationDebounce = setTimeout(function () { mutationDebounce = null; if (isActivelyProcessing) return; scheduleRetry(); }, 800); }); var observeTarget = document.querySelector('[data-testid=\'primaryColumn\']') || document.querySelector('main') || document.body; observer.observe(observeTarget, { childList: true, subtree: true }); var currentInterval = null; var noTargetRetryCount = 0; var NO_TARGET_RETRY_MAX = 5; var stepCallCount = 0; var processedTweets = new WeakSet(); function findNextTargetTweet() { var articles = document.querySelectorAll('article[data-testid=\'tweet\']'); for (var i = 0; i < articles.length; i++) { var article = articles[i]; if (!document.body.contains(article)) continue; if (processedTweets.has(article)) continue; var tweetDate = getTweetDate(article); if (useDateFilter) { if (tweetDate === null) continue; if (startDate !== null && tweetDate < startDate) continue; if (endDate !== null && tweetDate > endDate) continue; } return article; } return null; } function findMoreButton(article) { if (!article) return null; var btn = article.querySelector('button[data-testid=\'caret\']'); if (btn) return btn; var path = article.querySelector( 'button svg path[d=\'M3 12c0-1.1.9-2 2-2s2 .9 2 2-.9 2-2 2-2-.9-2-2zm9 2c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2zm7 0c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2z\']' ); if (path && path.closest) { var b = path.closest('button'); if (b) return b; } return null; } function findReportMenuItem() { var texts = ['ポストを報告', 'ポストを報告する', 'ツイートを報告', 'Report post', 'Report Tweet']; var el = findElementByText(document, '[role=\'menuitem\']', texts); if (el) return el; var span = findElementByText(document, 'span,div', texts); if (span && span.closest) { var parent = span.closest('[role=\'menuitem\'],button,a,div'); if (parent) return parent; } return null; } function findReportDialog() { var dialogs = document.querySelectorAll('div[role=\'dialog\']'); for (var i = 0; i < dialogs.length; i++) { var dlg = dialogs[i]; if (!document.body.contains(dlg)) continue; var h2List = dlg.querySelectorAll('h2'); for (var j = 0; j < h2List.length; j++) { var t = (h2List[j].innerText || h2List[j].textContent || '').trim(); if (!t) continue; if (t.indexOf('何を報告していますか？') !== -1 || t.indexOf('What do you want to report?') !== -1) { return dlg; } } } return null; } function findHateOption(root) { root = root || document; var texts = ['ヘイト、攻撃的な行為、または嫌がらせ', 'ヘイト、攻撃的な行為']; var label = findElementByText(root, 'label', texts); if (label) return label; var option = findElementByText(root, '[role=\'radio\'],[role=\'option\']', texts); if (option) return option; return null; } function findNextButton(root) { root = root || document; var btn = root.querySelector('button[data-testid=\'ChoiceSelectionNextButton\']'); if (btn) return btn; var texts = ['次へ', 'Next']; return findElementByText(root, 'button', texts); } function isVisible(el) { if (!el || !document.body.contains(el)) return false; if (el.offsetParent === null && el.tagName !== 'BODY') return false; var rects = el.getClientRects && el.getClientRects(); return rects && rects.length > 0 && rects[0].width > 0 && rects[0].height > 0; } function findCompleteButton(root) { root = root || document; var byTestId = root.querySelector('[data-testid=\'ocfSettingsListNextButton\']'); if (byTestId && isVisible(byTestId)) return byTestId; var texts = ['完了', 'Done', '送信', 'Submit']; var btn = findElementByText(root, 'button', texts); if (btn && isVisible(btn)) return btn; var el = findElementByText(root, 'span,div', texts); if (el) { var clickable = el.closest('button') || el.closest('[role=\'button\']'); if (clickable && isVisible(clickable)) return clickable; var outer = el.closest('[dir=\'ltr\']'); if (outer && isVisible(outer)) return outer; if (isVisible(el)) return el; return el.parentElement && isVisible(el.parentElement) ? el.parentElement : null; } return null; } function processTweet(article, onDone) { if (!document.body.contains(article)) { onDone(false); return; } try { article.scrollIntoView({ behavior: 'smooth', block: 'center' }); } catch (err) {} var moreBtn = findMoreButton(article); if (!moreBtn) { onDone(false); return; } try { moreBtn.click(); } catch (err) { onDone(false); return; } var menuAttempts = 0; function tryClickReport() { menuAttempts++; var reportItem = findReportMenuItem(); if (reportItem) { try { reportItem.click(); } catch (err) { onDone(false); return; } setTimeout(handleReportDialog, 700); return; } if (menuAttempts > 15) { onDone(false); return; } setTimeout(tryClickReport, 300); } setTimeout(tryClickReport, 400); function handleReportDialog() { var attempts = 0; function tryHandle() { attempts++; var dlg = findReportDialog(); var root = dlg || document; var hateOption = findHateOption(root); if (hateOption) { try { hateOption.click(); } catch (err) {} setTimeout(function () { var nextBtn = findNextButton(root); if (nextBtn && !nextBtn.disabled && nextBtn.getAttribute('aria-disabled') !== 'true') { try { nextBtn.click(); } catch (err) {} setTimeout(function () { tryClickComplete(); }, 600); return; } if (attempts > 20) { onDone(false); return; } setTimeout(tryHandle, 400); }, 300); return; } if (attempts > 20) { onDone(false); return; } setTimeout(tryHandle, 400); } tryHandle(); } function tryClickComplete() { var completeAttempts = 0; function attempt() { completeAttempts++; var completeBtn = findCompleteButton(document); if (completeBtn && !completeBtn.disabled && completeBtn.getAttribute('aria-disabled') !== 'true') { try { completeBtn.click(); } catch (err) {} setTimeout(function () { onDone(true); }, 500); return; } if (completeAttempts > 25) { onDone(true); return; } setTimeout(attempt, 400); } attempt(); } } stepFn = function step() { stepCallCount++; isActivelyProcessing = true; try { if (!document.body.contains(progressDiv)) { progressDiv = createProgressContainer(); } try { updateProgress(progressDiv, successCount, totalDisplayFn(), '実行中... (step:' + stepCallCount + '回目)'); } catch (err) {} if (maxCount > 0 && successCount >= maxCount) { if (reloadRetryTimeout) return; var totalDisplayDone = totalDisplayFn(); updateProgress(progressDiv, successCount, totalDisplayDone, '完了'); alert('通報処理が完了しました。'); return; } var targetTweet = findNextTargetTweet(); if (targetTweet) { processedTweets.add(targetTweet); processTweet(targetTweet, function (success) { if (success) { successCount++; noTargetRetryCount = 0; } var totalDisplay = totalDisplayFn(); var shouldContinue = (maxCount === 0 || successCount < maxCount); if (!shouldContinue) { if (reloadRetryTimeout) return; updateProgress(progressDiv, successCount, totalDisplay, '完了'); alert('通報処理が完了しました。'); return; } isActivelyProcessing = false; var rem = Math.floor(waitTime / 1000); currentInterval = setInterval(function () { try { updateProgress(progressDiv, successCount, totalDisplay, '次の操作まで: ' + rem + '秒'); rem--; if (rem < 0) { clearInterval(currentInterval); currentInterval = null; if (reloadRetryTimeout) { clearTimeout(reloadRetryTimeout); reloadRetryTimeout = null; } stepFn(); } } catch (err) { if (currentInterval) clearInterval(currentInterval); currentInterval = null; scheduleRetry(); } }, 1000); }); } else { var scrollEl = document.querySelector('[data-testid=\'primaryColumn\']') || document.querySelector('main'); var canScrollWindow = window.innerHeight + window.scrollY < document.body.scrollHeight; var canScrollEl = scrollEl && scrollEl.scrollHeight > scrollEl.clientHeight && scrollEl.scrollTop + scrollEl.clientHeight < scrollEl.scrollHeight - 50; if (canScrollWindow) { try { updateProgress(progressDiv, successCount, totalDisplayFn(), '次のポストを探しています...'); } catch (err) {} window.scrollBy(0, 800); setTimeout(function () { stepFn(); }, 250); } else if (canScrollEl) { try { updateProgress(progressDiv, successCount, totalDisplayFn(), '次のポストを探しています...'); } catch (err) {} scrollEl.scrollBy(0, 800); setTimeout(function () { stepFn(); }, 250); } else { var totalDisplay2 = totalDisplayFn(); setTimeout(function () { if (reloadRetryTimeout) return; if (noTargetRetryCount < NO_TARGET_RETRY_MAX) { noTargetRetryCount++; try { updateProgress( progressDiv, successCount, totalDisplay2, 'ターゲットなし: 再試行 ' + noTargetRetryCount + '/' + NO_TARGET_RETRY_MAX + '...' ); } catch (err) {} scheduleRetry(); return; } try { updateProgress(progressDiv, successCount, totalDisplay2, 'ターゲットなし: 完了'); alert('通報対象が見つからなかったため、処理を終了しました。'); } catch (err) { scheduleRetry(); } }, 1000); } } } catch (e) { console.error(e); if (currentInterval) try { clearInterval(currentInterval); } catch (err) {} scheduleRetry(); } }; stepFn(); } (function (callback) { var dlg = document.createElement('div'); dlg.id = 'xAlertSpamDialogRoot'; dlg.style.cssText = 'position:fixed;inset:0;background:rgba(0,0,0,0.5);z-index:2147483647;display:flex;align-items:center;justify-content:center;font-family:Arial,sans-serif;'; dlg.innerHTML = '<div style=\'position:relative;background:#f9f9f9;padding:0;border-radius:10px;box-shadow:0 4px 20px rgba(0,0,0,0.3);width:320px;max-width:95vw;overflow:hidden;\'>' + '<div style=\'background:#dc3545;padding:10px;text-align:center;\'><span style=\'color:#fff;font-size:18px;\'>X通報ツール</span></div>' + '<div style=\'padding:15px;\'>' + '<div style=\'font-size:14px;color:#444;text-align:left;margin-bottom:15px;line-height:1.4;\'>' + 'タイムライン上のポストに対して、<br/>「ヘイト、攻撃的な行為、または嫌がらせ」として通報を自動で行います。<br/>' + '期間を必要に応じて指定してください。' + '</div>' + '<label style=\'display:block;color:#555;margin-bottom:5px;\'>開始日（任意）：</label>' + '<input type=\'date\' id=\'xAlertStartDate\' style=\'width:100%;padding:8px;margin-bottom:15px;border:1px solid #ccc;border-radius:5px;box-sizing:border-box;text-align:center;\'/>' + '<label style=\'display:block;color:#555;margin-bottom:5px;\'>終了日（任意）：</label>' + '<input type=\'date\' id=\'xAlertEndDate\' style=\'width:100%;padding:8px;margin-bottom:15px;border:1px solid #ccc;border-radius:5px;box-sizing:border-box;text-align:center;\'/>' + '<label style=\'display:block;color:#555;margin-bottom:5px;\'>待機時間 (秒)：</label>' + '<input type=\'number\' id=\'xAlertWaitTime\' value=\'15\' min=\'1\' style=\'width:100%;padding:8px;margin-bottom:20px;border:1px solid #ccc;border-radius:5px;box-sizing:border-box;text-align:center;\'/>' + '<button type=\'button\' id=\'xAlertConfirmBtn\' style=\'width:100%;padding:10px;background:#dc3545;color:#fff;border:none;border-radius:5px;cursor:pointer;font-size:16px;margin-bottom:10px;\'>開始</button>' + '<button type=\'button\' id=\'xAlertCloseBtn\' style=\'width:100%;padding:10px;background:#6c757d;color:#fff;border:none;border-radius:5px;cursor:pointer;font-size:16px;\'>閉じる</button>' + '</div></div>'; document.body.appendChild(dlg); var btnStart = dlg.querySelector('#xAlertConfirmBtn'); var btnClose = dlg.querySelector('#xAlertCloseBtn'); var inputStart = dlg.querySelector('#xAlertStartDate'); var inputEnd = dlg.querySelector('#xAlertEndDate'); var inputWait = dlg.querySelector('#xAlertWaitTime'); if (btnStart) { btnStart.addEventListener('click', function () { var startDate = (inputStart && inputStart.value) ? inputStart.value.trim() : ''; var endDate = (inputEnd && inputEnd.value) ? inputEnd.value.trim() : ''; var waitTime = (inputWait && inputWait.value) ? inputWait.value : '15'; if (dlg.parentNode) dlg.parentNode.removeChild(dlg); callback(startDate, endDate, waitTime, ''); }); } if (btnClose) { btnClose.addEventListener('click', function () { if (dlg.parentNode) dlg.parentNode.removeChild(dlg); }); } })(function (startDate, endDate, waitTime, maxCount) { run(startDate, endDate, waitTime, maxCount); }); })();